package Chatbox.GUI;

import javafx.application.Application;

public class TCP extends MainMenu {
    public static void main(String[] args) {
        launchProtocol = "TCP";
        Application.launch(TCP.class, args);
    }
}
