package Chatbox.GUI;

import javafx.application.Application;

public class UDP extends MainMenu {
    public static void main(String[] args) {
        launchProtocol = "UDP";
        Application.launch(UDP.class, args);
    }
}
